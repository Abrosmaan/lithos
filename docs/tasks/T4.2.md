# T4.2 — Сборка и полевой тест

Статус: **ожидает человека** · 2026-09-08. Код прототипа готов (T0.1–T4.1 закоммичены), сборка и полевой тест требуют аккаунтов и телефона.

## Что нужно сделать руками (по порядку)

1. ~~Exposed schemas~~ — **сделано 2026-09-08** (SQL `alter role authenticator set pgrst.db_schemas`, дашборд не сохранял). `pnpm --filter mobile check:backend` — все `[OK]`.
2. ~~Секреты на VPS и запуск воркера~~ — **сделано 2026-09-08**: `lithos-worker` работает на flat-vps (Node 22, 199 МБ), лог `worker started` / `queues empty`. Передеплой: rsync + `docker compose up -d --build worker` (INFRA.md §4).
3. **Google Gemini ключ** → `.env` (`GOOGLE_GENERATIVE_AI_API_KEY`) локально и на VPS; проверка `pnpm --filter worker exec tsx src/llm/smoke.ts --provider google`.
4. ~~Golden set скачан и прогнан на Anthropic~~ — **сделано 2026-09-08** (baseline в `docs/tasks/T2.4.md`). Осталось: после Google-ключа `pnpm eval --concurrency 3` (≈ $1.7), решение по Main (Sonnet 8 с vs цель 5 с), обновить `STAGE_*` в `.env` на VPS, `docker compose restart worker`.
5. **Проверка в Expo Go** (без сборки): `pnpm --filter mobile start` → QR → сценарий dev-plan §0 пункты 1–6. Чек-листы по экранам — в `docs/tasks/T1.4.md`, `T2.2.md`, `T3.1–T3.3.md`.
6. **EAS-сборка**: `npm i -g eas-cli && eas login && cd apps/mobile && eas init && eas build --profile preview --platform ios` (TestFlight — нужен Apple Developer аккаунт) и `--platform android` (APK). `eas.json` уже есть; `EXPO_PUBLIC_*` задать через `eas env:create` для окружения preview. Для Android-карты — Google Maps API key в `app.json` (`android.config.googleMaps.apiKey`).
7. **Полевой тест**: 20 сканов подряд на пляже, таблица ниже; 3 камня переснять по 3 раза — стабильность тира. Пункт 7 сценария (отключить основной ключ) — на VPS временно подменить `ANTHROPIC_API_KEY` в `.env` и перезапустить воркер: карточка должна прийти через Google с пометкой «предварительно».

## Таблица полевого теста (заполнить)

| # | Камень (ожидание) | Порода (вердикт) | Тир | Время до 1-го экрана, с | Стоимость, $ | Совпало? |
|---|---|---|---|---|---|---|
| 1 | | | | | | |
| … | | | | | | |

Стабильность тира: камень A — тиры 3 пересъёмок: …; B: …; C: …

## Критерии приёмки (dev-plan)

- ≥ 18 из 20 сканов дали карточку за ≤ 5 с. **Риск**: Sonnet 5 на 3 фото отвечает 8–9 с (T2.1) — вероятно, потребуется Gemini Flash на Main по итогам `pnpm eval`.
- Стабильность тира ≥ 80 %.
- Средняя стоимость ≤ $0.03.

## Что уже подтверждено без телефона

- Конвейер end-to-end на реальной БД/Storage/Anthropic (T2.1, T4.1): gate → main → escalation → карточка, идемпотентность после SIGKILL, pHash-кэш, бюджетный предохранитель, breaker при отозванном ключе, DLQ.
- Бандл клиента собирается (`expo export`), в нём только публичные ключи.
- Все тесты: shared 63, worker 146, mobile 85; typecheck и lint зелёные.
