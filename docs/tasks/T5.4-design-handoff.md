# T5.4 — Перевод экранов на дизайн прототипа: план-передача

Статус: **в работе, передано другой сессии** · 2026-09-09 · ветка `design-refresh`, PR #1 (Abrosmaan/lithos).

Эта страница — самодостаточное задание. Исполнитель (Claude Opus/Sonnet в новой сессии) читает её первой, затем `CLAUDE.md`, `docs/design/DESIGN_SYSTEM.md`, и только потом код.

## 1. Источники истины

1. `docs/design/Lithos App.dc.html` — кликабельный прототип. Разметка экранов: строки 60–910; данные/тексты: 930–1494. Точные значения (цвета, размеры, шрифты) брать отсюда.
2. `docs/design/DESIGN_SYSTEM.md` — выжимка: токены, типографика, компоненты, 14 экранов с состояниями. При расхождении с прототипом прав прототип.
3. `docs/DESIGN_BRIEF.md` — поведение экранов и тексты отказов (функциональность не меняется).
4. Что в прототипе не учтено, но уже есть в коде и должно остаться: список кандидатов с процентами на результате и карточке (`IdentificationList`, `docs/tasks/T5.1-identification-ui.md`) — оформить в языке прототипа (primary Playfair, остальные Golos `textMuted`, полоски 3px, проценты mono).

Посмотреть прототип живьём: `cd docs/design && python3 -m http.server 8787` → `http://127.0.0.1:8787/Lithos%20App.dc.html`; под телефоном — чипы переключения состояний.

## 2. Что уже сделано (закоммичено в `design-refresh`)

- `apps/mobile/src/theme.ts` — токены `colors` (bg/surface/surface2/surfaceAlt/surfaceDim/track/accent/accentBright/accentSoft/accentText/accentTint/accentBorder/text/textSoft/textMuted/textDim/textFaint/chipText/danger*/gold*/tabBar/overlay/scrim), `fonts` (имена expo-font), `type` (готовые TextStyle: h1/h2/h3/tierName/tileName/lore/body/bodyStrong/small/caption/button/buttonSm/sectionLabel/brand/mono/monoId/scoreBig/scoreMid/statValue/stepNumber), `density`, `radius`, `tierColors`, `mapColors`.
- Шрифты: `@expo-google-fonts/{playfair-display,golos-text,jetbrains-mono}` загружаются в `App.tsx` через `useFonts`; fallback на системные при ошибке.
- Базовые компоненты (использовать, не дублировать): `components/ui.tsx` — `SectionLabel`, `StepNumber`, `StepRow`, `CardFrame`, `TierSeam` (анимированный шов), `TierLine` («тир и цифра»), `Note` (gold/danger/neutral), `DeltaPill`, `FactRow`, `KeyValue`, `Divider`, `Pill`, `PhotoPlaceholder`, `LinkRow`, `Chevron`; `BigButton` (primary/secondary/danger/ghost), `Chip` (filter с count / sort), `Section` (+`Line`).
- `.eslint` игнорирует `docs/design/**`.

## 3. Что начато и оборвано (в рабочем дереве, частично; см. `git status`)

Агенты успели создать/изменить, но не довели; **перед продолжением прочитать каждый файл и решить: доделать или переписать**:

| Файл | Состояние |
|---|---|
| `screens/WelcomeScreen.tsx` (новый) | написан; ошибка типов: маршрут `Welcome` не добавлен в `navigation/types.ts` / `RootNavigator.tsx` |
| `screens/CameraScreen.tsx` | переписан под прототип; ошибка `StyleSheet.absoluteFillObject` → `StyleSheet.absoluteFill` |
| `screens/ReviewScreen.tsx` | переписан под прототип (проверить сегменты/баннеры) |
| `screens/ResultScreen.tsx` | частично; ссылается на `TEXT.slow`, которого нет в `lib/result-text.ts` |
| `screens/CardScreen.tsx` | частично переписан |
| `components/CardTile.tsx`, `TabNavigator.tsx`, `TabIcons.tsx` | переписаны (иконки-примитивы) — проверить |
| `components/{Analyzing,FadeIn,FirstRunOverlay,Refusal,StatTile,SettingsList,Segmented,PhotoThumb}.tsx` | новые/обновлённые компоненты — проверить соответствие DESIGN_SYSTEM |
| `lib/{compare.ts,screen-text.ts,settings.ts}` (новые), `lib/{prefs,profile,result-text,auth}.ts` (изменены) | логика для Compare/Welcome/Settings; ошибка в `screen-text.ts:136` — тип слоя `'nogeo'` не входит в союз `quality|shape|place|composition` |
| **Не начато** | `CompareScreen.tsx`, `SafetyScreen.tsx` (рестайл), `CollectionScreen.tsx`, `DiaryScreen.tsx`, `MapScreen.tsx`, `ProfileScreen.tsx` |

Текущие ошибки `pnpm --filter mobile typecheck` — 4 (перечислены выше). Тесты (99) и lint были зелёные до правок экранов.

## 4. План работ (последовательно, один исполнитель)

### Шаг 0. Стабилизация (30 мин)
- Починить 4 ошибки typecheck; добавить маршруты `Welcome` и `Compare` в `navigation/types.ts` и `RootNavigator.tsx` (Welcome — до `Tabs`, показывается один раз по флагу в `lib/prefs.ts`; Compare — экран-стек как Result).
- `pnpm --filter mobile typecheck && test && pnpm lint` — зелёные. Коммит `wip(design): stabilize`.

### Шаг 1. Поток сканирования (экраны 1–8, 14 DESIGN_SYSTEM.md)
- Welcome, Camera (нет разрешения / first-run оверлей / тост / нижний блок с «n / 3», спуском 74px, «Далее»), Review (миниатюры 94px, «масштаб», Segmented с mono-подписями, Note без гео, плашка лимита, «Определить»/«Лимит исчерпан»), Analyzing (кольцо + 5 ступеней), Result-карточка (`CardFrame` → фото 206 + «Уточняем» + `TierSeam` → имя Playfair 24 + порода + `IdentificationList` → `TierLine` → `Note` → `DeltaPill` → 3 `FactRow` → `Divider` → лор; кнопки; анимации появления), Refusal (глифы и тексты из прототипа 1058–1066, 1312–1314), Compare (до/после раскола из разницы `score_breakdown` родителя и ребёнка; `lib/compare.ts` уже начат), Safety.
- Правило: тексты дословно из прототипа/DESIGN_BRIEF; никаких чисел-порогов в клиенте.

### Шаг 2. Коллекция и карточки (экраны 9–13 + таб-бар)
- TabNavigator с `TabIcons` (примитивы, без эмодзи), Card (галерея 300×224 snap, имя/своё имя/ID mono, блок тира с плашками и «Разбор score», секции `Section`+`KeyValue`, история версий, `LinkRow` «Дневник этого места», кнопки), Collection (H1 + mono-счётчик, вход в дневник, чипы с count, сортировка, пустое состояние, сетка `CardTile`), CardTile (md/sm/xs с швом и флагом), Diary, Map (маркеры-кружки, легенда на `track`), Profile (StatTile ×3, распределение по тирам, highlights, витрина 3×, блоки «Аккаунт» с честной заглушкой входа через Apple и «Настройки» по списку прототипа 1469–1477 с реальными статусами разрешений и «Удалить все данные» с подтверждением; `lib/settings.ts` начат).

### Шаг 3. Приёмка
- Гейты: `pnpm typecheck`, `pnpm lint`, `pnpm --filter mobile test`, `cd apps/mobile && npx expo export --platform ios` (затем удалить `dist`).
- Ревью субагентом code-reviewer против `DESIGN_SYSTEM.md`: не осталось `fontWeight` без `fontFamily`, эмодзи, цветов вне `theme.ts`, балансовых чисел.
- Визуально: `pnpm --filter mobile start` → Expo Go (телефон) или `i` (симулятор iPhone 17 Pro уже загружен); сверить каждый экран с прототипом по чипам состояний. Расхождения и осознанные отклонения — в артефакт.
- Артефакты: `docs/tasks/T5.2-design-flow.md` (шаг 1), `T5.3-design-cards.md` (шаг 2). Коммиты в `design-refresh`, push, PR #1 обновляется автоматически; в описании PR снять пометку WIP.

## 5. Ограничения и правила
- Не менять `packages/shared`, `apps/worker`, миграции. Только `apps/mobile/**` и `docs/tasks/**`.
- Балансовые числа (тиры, пороги, лимиты, `SHOWCASE_MAX`) — только из `@lithos/shared`.
- Секреты не читать; в бандл — только `EXPO_PUBLIC_*`.
- Коммит после каждого зелёного шага, сообщения на английском. Не мержить PR без зелёных гейтов.
